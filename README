# JSON Resume Generator

This project aims to make an easy to edit resume in the JSON structure that can be dynamically changed depending on the application. Elements can be shown, hidden, and rearranged before saving to PDF.

![](example.png)

Above is an example of what default formatting looks like, but important CSS rules are stored in [`format.css`](format.css) and can be changed easily.

## Usage

Visit [`instructions.md`](instructions.md) to see how to use